# Upload

The `Upload` module allows you to upload files from a directory and its subdirectories to AWS S3 and Google Cloud Storage. It supports configurable file types for each storage service.

## Installation

```bash
pip install Upload
